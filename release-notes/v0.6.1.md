## Fixed

- **The Rota list drew white boxes on the dark theme.** Every field on that
  screen — room codes, names, phone numbers — fell back to the browser's default
  white-on-black instead of the app's own styling. It looked broken because it
  was.
- **The Weight dropdown cut off its own labels**, reading "Full tur" and
  "Half —" with the arrow sitting on top of the text.
- The Name column clipped its placeholder to "optior".

Nothing about how a rota works has changed, and nothing needs re-doing. If your
Rota screens looked wrong in v0.6.0, they were.
